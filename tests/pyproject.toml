[tool.poetry]
name = "nitter-tests"
version = "0.0.0"
package-mode = false

[tool.poetry.dependencies]
python = "^3.14"
seleniumbase = "4.46.5"

[tool.pytest.ini_options]
addopts = "--timeout_multiplier=3"
